# Option 1: Install the old adapter
npm install @next-auth/mongodb-adapter

# Option 2: If that fails, use force
npm install @next-auth/mongodb-adapter --force

# Option 3: Use the new adapter (recommended)
npm install @auth/mongodb-adapter mongodb@^6